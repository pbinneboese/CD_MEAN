<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Mongoose Edit Critter</title>
    <link rel="stylesheet" type="text/css" href="/style.css">
  </head>
  <body>
    <p><img src="/aardvark.jpg" alt="aardvark"></p>
    <p>Aaron, our first critter</p>
    <h3>Critter Current Details</h3>
    <p>ID: <%= critter.ident %></p>
    <p>Name: <%= critter.name %></p>
    <p>Age: <%= critter.age %></p>
    <p>Weight: <%= critter.weight %></p>
    <p>Diet: <%= critter.diet %></p>
    <h3>Update Critter Details</h3>
    <form class="form" action="/<%= critter.ident %>/critter" method="post">
      <!-- ID stays the same, other fields can change -->
      <!-- <p>ID (between 1 and 1000):<input type="number" name="ident" min="1" max="1000"></p> -->
      <p>Name (up to 40 characters):<input type="text" name="name" value="<%= critter.name %>"></p>
      <p>Age in years (between 1 and 100):<input type="number" name="age" value="<%= critter.age %>" min="1" max="100"></p>
      <p>Weight in pounds (between 1 and 100):<input type="number" value="<%= critter.weight %>" name="weight" min="1" max="100"></p>
      <p>Diet:<textarea name="diet" rows="4" cols="60" value="<%= critter.diet %>"></textarea></p>
      <p><button type="submit" name="save">Save</button></p>
    </form>
    <p></p>
    <form class="button" action="/" method="get">
      <button type="submit" name="back">Home</button>
    </form>
    <p></p>
  </body>
</html>
